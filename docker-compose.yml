version: '3.3'
services:
  db:
    image: postgres
    restart: unless-stopped 
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    ports:
      - '${DB_PORT}:5432'
    volumes: 
      - ./db:/var/lib/postgresql/data
  web:
    image: sebivenlo/prj1-web
    restart: unless-stopped 
    user: ${UID}:${GID}
    ports:
      - '${WEB_PORT}:80'
    volumes:
      - '${WEB_VOLUME}:/var/www/html'
    depends_on:
      - db
volumes:
  prj1_db:
    driver: local
  prj1_web:
    driver: local
